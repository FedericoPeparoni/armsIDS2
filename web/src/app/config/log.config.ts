/**
 * Override functionality for log provider.
 */

/** @ngInject */
export function LogConfig($logProvider: angular.ILogProvider): void {
    // enable log, uncomment for development purposes only
    // $logProvider.debugEnabled(true);
}
